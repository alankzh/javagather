includeDir 'account'
includeDir 'netty_action'
includeDir 'base'
includeDir 'comment'
includeDir 'rediskeys'

def includeDir(String dir) {
    def file = rootDir.toPath().resolve("$dir/deps.gradle").toFile()
    if (file.exists()) {
        apply from: file
    }
}